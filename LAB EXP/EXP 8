# ============================================================
# TRAVELLING SALESMAN PROBLEM (TSP)
# Name    : S DURGA PRASAD
# Reg No  : 192472091
# ============================================================

from itertools import permutations


# Function to solve TSP
def travelling_salesman(graph, start):

    cities = list(graph.keys())

    # Remove starting city from the list
    cities.remove(start)

    shortest_route = None
    minimum_cost = float("inf")

    # Generate all possible routes
    for route in permutations(cities):

        # Create complete route
        current_route = (start,) + route + (start,)

        # Calculate total cost
        cost = 0

        for i in range(len(current_route) - 1):
            city1 = current_route[i]
            city2 = current_route[i + 1]

            cost += graph[city1][city2]

        # Check for minimum cost
        if cost < minimum_cost:
            minimum_cost = cost
            shortest_route = current_route

    return shortest_route, minimum_cost


# ============================================================
# Main Program
# ============================================================

print("==============================================")
print("     TRAVELLING SALESMAN PROBLEM (TSP)")
print("==============================================")
print("Name   : S DURGA PRASAD")
print("Reg No : 192472091")
print("==============================================")


# Distance/cost matrix
graph = {
    "A": {"A": 0, "B": 10, "C": 15, "D": 20},
    "B": {"A": 10, "B": 0, "C": 35, "D": 25},
    "C": {"A": 15, "B": 35, "C": 0, "D": 30},
    "D": {"A": 20, "B": 25, "C": 30, "D": 0}
}


# Starting city
start_city = "A"


# Display the distance matrix
print("\nDistance Matrix:")
print("----------------------------------------------")

print("      A   B   C   D")

for city in graph:
    print(
        city, " : ",
        graph[city]["A"],
        graph[city]["B"],
        graph[city]["C"],
        graph[city]["D"]
    )


# Solve TSP
shortest_route, minimum_cost = travelling_salesman(
    graph,
    start_city
)


# Display result
print("\nStarting City:", start_city)

print("\nShortest Route:")
print("----------------------------------------------")

print(" -> ".join(shortest_route))

print("----------------------------------------------")
print("Minimum Total Cost:", minimum_cost)

print("----------------------------------------------")
print("TSP Solved Successfully.")
Output
==============================================
     TRAVELLING SALESMAN PROBLEM (TSP)
==============================================
Name   : S DURGA PRASAD
Reg No : 192472091
==============================================

Distance Matrix:
----------------------------------------------
      A   B   C   D
A  :  0 10 15 20
B  :  10 0 35 25
C  :  15 35 0 30
D  :  20 25 30 0

Starting City: A

Shortest Route:
----------------------------------------------
A -> B -> D -> C -> A
----------------------------------------------
Minimum Total Cost: 80
----------------------------------------------
TSP Solved Successfully.
